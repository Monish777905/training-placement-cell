#include <stdio.h>

int main() {
    int num = 15;

    if (num < 10) {
        printf("Number is less than 10.\n");
    } else if (num < 20) {
        printf("Number is between 10 and 19.\n");
    } else {
        printf("Number is 20 or greater.\n");
    }
    
    return 0;
}
